{
  "name": "letsplay-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply letsplay-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply letsplay-db --remote",
    "test:hardening": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.113.0"
  }
}
